name: "Deploy: BAYSTAR"

on:
  repository_dispatch:
    types: [trigger_baystar_deployment]
  workflow_dispatch:

jobs:
  merging-process:
    uses: ./.github/workflows/_auto_merge.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      base: "tenant/baystar"
      compare: "main"
      deployment_name: "BAYSTAR"
      dashboard_url: "https://asint-payg-development.cicd.cfapps.eu10.hana.ondemand.com/ui/index.html#"
    secrets: inherit
