name: "Deploy: HSC Non-Prod DC AddIn"

on:
  repository_dispatch:
    types: [trigger_hsc_non_prod_dc_addin_deployment]

jobs:
  merging-process:
    uses: ./.github/workflows/_auto_merge.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      base: "tenant/hemlock-non-prod-dc-addin"
      compare: "main-dc-addin"
      deployment_name: "HSC Non-Prod DC AddIn"
      dashboard_url: "https://asint-payg-development.cicd.cfapps.eu10.hana.ondemand.com/ui/index.html#"
    secrets: inherit
