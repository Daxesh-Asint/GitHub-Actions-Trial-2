name: "Deploy: Indorama Prod 900 DC AddIn"

on:
  repository_dispatch:
    types: [trigger_indorama_prod_900_dc_addin_deployment]

jobs:
  merging-process:
    uses: ./.github/workflows/_auto_merge.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      base: "tenant/indorama-prod-900-dc-addin"
      compare: "tenant/indorama-qa-233-dc-addin"
      deployment_name: "Indorama Prod 900 DC AddIn"
      dashboard_url: "https://asint-payg-development.cicd.cfapps.eu10.hana.ondemand.com/ui/index.html#"
    secrets: inherit
