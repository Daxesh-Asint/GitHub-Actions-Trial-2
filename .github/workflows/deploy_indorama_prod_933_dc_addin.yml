name: "Deploy: Indorama Prod 933 DC AddIn"

on:
  repository_dispatch:
    types: [trigger_indorama_prod_933_dc_addin_deployment]

jobs:
  merging-process:
    uses: ./.github/workflows/_auto_merge.yml
    permissions:
      contents: write
      pull-requests: write
    with:
      base: "tenant/indorama-prod-933-dc-addin"
      compare: "tenant/indorama-qa-234-dc-addin"
      deployment_name: "Indorama Prod 933 DC AddIn"
      dashboard_url: "https://asint-payg-development.cicd.cfapps.eu10.hana.ondemand.com/ui/index.html#"
    secrets: inherit
